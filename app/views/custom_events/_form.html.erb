<%= form_with(model: custom_event) do |form| %>
	<div class="form-container">
		<% if custom_event.errors.any? %>
		<div class="error-messages">
			<h2><%= pluralize(custom_event.errors.count, "error") %> prohibited this custom_event
				from being saved:</h2>

			<ul>
				<% custom_event.errors.each do |error| %>
				<li><%= error.full_message %></li>
				<% end %>
			</ul>
		</div>
		<% end %>

		<div class="form-group">
				<%= form.text_field :name %>
		</div>

		<div class="form-group">
				<%= form.textarea :description %>
		</div>

		<div class="form-group">
				<%= form.text_field :custom_scenario_id %>
		</div>

		<div class="form-group">
				<%= form.text_field :name %>
		</div>

		<div class="form-group">
				<%= form.text_field :trigger_keywords %>
		</div>

		<div class="form-group">
				<%= form.number_field :sequence_order %>
		</div>

		<div class="actions">
	    <%= form.submit class: 'btn btn-primary' %>
	  </div>
	</div>
<% end %>
