<%= form_with(model: predefined_event) do |form| %>
	<div class="form-container">
		<% if predefined_event.errors.any? %>
		<div class="error-messages">
			<h2><%= pluralize(predefined_event.errors.count, "error") %> prohibited this predefined_event
				from being saved:</h2>

			<ul>
				<% predefined_event.errors.each do |error| %>
				<li><%= error.full_message %></li>
				<% end %>
			</ul>
		</div>
		<% end %>

		<div class="form-group">
				<%= form.text_field :name %>
		</div>

		<div class="form-group">
				<%= form.textarea :description %>
		</div>

		<div class="form-group">
				<%= form.text_field :predefined_scenario_id %>
		</div>

		<div class="actions">
	    <%= form.submit class: 'btn btn-primary' %>
	  </div>
	</div>
<% end %>
