<%= form_with(model: exercise_objective_assignment) do |form| %>
	<div class="form-container">
		<% if exercise_objective_assignment.errors.any? %>
		<div class="error-messages">
			<h2><%= pluralize(exercise_objective_assignment.errors.count, "error") %> prohibited this exercise_objective_assignment
				from being saved:</h2>

			<ul>
				<% exercise_objective_assignment.errors.each do |error| %>
				<li><%= error.full_message %></li>
				<% end %>
			</ul>
		</div>
		<% end %>

		<div class="form-group">
				<%= form.text_field :team_id %>
		</div>

		<div class="form-group">
				<%= form.text_field :exercise_id %>
		</div>

		<div class="form-group">
				<%= form.text_field :exercise_objective_id %>
		</div>

		<div class="form-group">
				<%= form.number_field :priority %>
		</div>

		<div class="actions">
	    <%= form.submit class: 'btn btn-primary' %>
	  </div>
	</div>
<% end %>
