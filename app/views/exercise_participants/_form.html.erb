<%= form_with(model: exercise_participant) do |form| %>
	<div class="form-container">
		<% if exercise_participant.errors.any? %>
		<div class="error-messages">
			<h2><%= pluralize(exercise_participant.errors.count, "error") %> prohibited this exercise_participant
				from being saved:</h2>

			<ul>
				<% exercise_participant.errors.each do |error| %>
				<li><%= error.full_message %></li>
				<% end %>
			</ul>
		</div>
		<% end %>
		<div class="form-group">
				<%= form.text_field :exercise_id %>
		</div>

		<div class="form-group">
				<%= form.text_field :name %>
		</div>

		<div class="form-group">
				<%= form.text_field :user_id %>
		</div>

		<div class="form-group">
				<%= form.text_field :role %>
		</div>

		<div class="actions">
	    <%= form.submit class: 'btn btn-primary' %>
	  </div>
	</div>
<% end %>
